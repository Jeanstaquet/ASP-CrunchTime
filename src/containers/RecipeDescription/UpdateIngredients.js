import { useState } from 'react'

const UpdateIngredients = ({ id, onUpdate }) => {
  const [text, setText] = useState('')
  const [day, setDay] = useState('')
  const [reminder, setReminder] = useState(false)

  

  return (
    
    
  )
}

export default UpdateIngredients